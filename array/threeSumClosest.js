let threeSumClosest = (nums, target) => {
    let total = 0;

    for (let i = 0; i < nums.length; i ++) {
        let number = nums[i];
        if (target === number) {
            nums.slice()

    }
}
    console.log(nums);
}

threeSumClosest([-1,2,1,-4], 1);
